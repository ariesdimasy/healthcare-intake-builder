{
  "name": "@healthcare/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Healthcare Intake Builder - Python FastAPI Backend",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check .",
    "build": "echo 'Python: tidak ada build step'"
  }
}
